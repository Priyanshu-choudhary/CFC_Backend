# Docker/EC2 deployment profile — set SPRING_PROFILES_ACTIVE=docker
spring.data.mongodb.uri=${MONGODB_URI:mongodb://mongodb:27017/cfc_db}

upload.dir=/home/ubuntu/uploads
spring.web.resources.static-locations=file:/uploads/,classpath:/static/

server.ssl.enabled=false
server.port=9090
